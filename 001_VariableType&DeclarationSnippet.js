// 'var' (older way, discouraged in modern JavaScript)
var x = 10; // 'x' is dynamically typed (no explicit type)

// 'let' (preferred for variable declaration)
let y = 20; // 'y' is dynamically typed (no explicit type)

// 'const'
const z = 30; // 'z' is dynamically typed (no explicit type)

